# Custom context

It is possible to create customization of the context.  
Given a model is possible to use a **Modelfile** to change some parameters and add a context.  



## Analysisn of Modelfile

```
# Modelfile generated by "ollama show"
# To build a new Modelfile based on this, replace FROM with:
# FROM dolphin-llama3:8b

FROM E:\Ollama models\blobs\sha256-ea025c107c1c3e5a87380b25e205db3b19a4d2ed56f1954f531573a963621580
TEMPLATE "{{ if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}{{ if .Prompt }}<|im_start|>user
{{ .Prompt }}<|im_end|>
{{ end }}<|im_start|>assistant
{{ .Response }}<|im_end|>
```



{{ .Prompt }}: This is a placeholder. It tells Ollama: "Insert the user's message exactly here."

Qwen3.5 specific instructions
RENDERER qwen3.5 : specify the internal formatting engine
PARSER qwen3.5 : specify how to handle special calls, history etc...

PARAMETER presence_penalty 1.5 : (0 - no effect) indicates how much is the model penalized if it repeats itself 0.1 means it reuse same tokens (phrases) , 10 it try to avoid to repeat completely, also responding with non-sense.

PARAMETER temperature 1: randomness, 0.1-0.3: focused and deterministic, 1.0-2.0 creative and surprising  
  
PARAMETER top_k 20 : limit the "choice pool" of tokens for the response  (10-100+)  
top_k 20 tells the model: "Ignore all words except the 20 most likely options. Then, pick the next word from only those 20."  
  
top_p 0.95 : nucleus sampling, cumulative probability
top_p 0.95 tells the model: "Pick the smallest set of words whose combined probability adds up to 95%. Then, choose the next word only from that set."
0.5 is very picky and will use much less words, while .95 will use a lot of "unrelated/less probable" words

PARAMETER num_predict 512
This limits the number of tokens in the response, to limit overthinking behaviour


SYSTEM : Define the Role or Rules of the model

  
{{ .System }} print out the "SYSTEM" Role or Rules  
{{ .Prompt }} print out the user input  
{{ .Messages }} print out the chat history
{{ .Role }} Role of the message
{{ .Content }} text of the message
{{ $.IsThinkSet }} true/false, as per el argument "think"
{{ .Thinking }} is the "reasoning" , it doesn't exists in all models (DeepSeek has it), some models mix the "reasoning" inside the content.

## Spanish teacher model

Create a Modelfile from an existing model:  
```sh
cd /e/Ollama\ models
#ollama show qwen3.5:4b --modelfile > espanol-profesora.qwen3.5-4b.v1
ollama show deepseek-r1-8b -f espanol-profesora.deepseek-r1-8b.v1
#code espanol-profesora.qwen3.5-4b.v1
code espanol-profesora.deepseek-r1-8b.v1
```

Create a model from a modelfile
```sh
cd /e/Ollama\ models
#ollama create espanol-profesora.qwen3.5-4b:1 -f espanol-profesora.qwen3.5-4b.v1
#ollama create espanol-profesora.qwen3.5-4b:2 -f espanol-profesora.qwen3.5-4b.v2
ollama create espanol-profesora.deepseek-r1-8b:1 -f espanol-profesora.deepseek-r1-8b.v1
ollama run espanol-profesora.qwen3.5-4b.v1
```

ollama create espanol-profesora.deepseek-r1-8b:1 -f espanol-profesora.deepseek-r1-8b.v1


```bash
cd /e/Ollama\ models
ollama show llama3.2 --modelfile > llama3.2-4k-fast.Modelfile
code  llama3.2-4k-fast.Modelfile
```


```bash
ollama create espanol-profesora-1  -f llama3.2-4k-fast.Modelfile
ollama list | grep espanol
ollama run espanol-profesora-1
``` 
